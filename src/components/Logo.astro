---
interface Props {
  as?: keyof HTMLElementTagNameMap;
  class?: string;
}

const { as: Tag = 'div', class: className = '' } = Astro.props;
---

<Tag class={className}>
  <span class="text-acc">[x]</span><span class="text-ink">mdtask</span>
</Tag>
